# 05 · API 账号开通清单(执行手册)

## 一、DeepSeek ✅ 已完成

- 平台:platform.deepseek.com → API keys 页创建
- 凭证:单个 `sk-` 开头的 key
- 状态:已跑通,demo 服务端可用
- ⚠️ **上线前换新 key**(当前 key 曾在协作过程中明文出现),并在控制台设用量告警

## 二、讯飞开放平台(语音评测 ISE)⬅ 现在要做

### 关键差异:讯飞给的是三件套

DeepSeek 是一个 key,讯飞是 **APPID + APIKey + APISecret** 三个 32 位字符串,三个都要,缺一不可。

### 开通步骤

| # | 步骤 | 说明 |
|---|---|---|
| 1 | 注册账号 | [www.xfyun.cn](https://www.xfyun.cn) 右上角注册 |
| 2 | **实名认证** | 控制台 → 实名认证。**免费额度按认证等级发放,企业认证额度更高** |
| 3 | 创建应用 | 控制台 → 我的应用 → 创建新应用(应用名如「书山英语」,行业选教育) |
| 4 | 添加服务 | 在应用里找到**语音评测(流式版)**并开通 |
| 5 | 领免费额度 | 到[语音评测产品页](https://www.xfyun.cn/services/ise)领取免费服务量;**SDK 版与 WebAPI 版分开领、额度不互通**,我们用 WebAPI 版 |
| 6 | 复制三件套 | 应用详情页可见 APPID / APIKey / APISecret,填入 `server/.env.local` |
| 7 | 关掉 IP 白名单 | 控制台服务页面默认可能开启 IP 白名单,开发阶段先关闭(或加本机外网 IP),否则握手直接失败 |

**创建应用后默认有每日 500 次调用额度**,足够开发调试;正式试点前再领/买正式服务量。

### ⚠️ 前置卡点:企业认证要营业执照

商用交付、对公付款、开发票都需要**企业实名认证**,而企业认证要提交营业执照。
→ 这正好是 [`docs/strategy/01-sales-playbook.md`](../../docs/strategy/01-sales-playbook.md) 里「第一堵墙:身份之困」那条:**公司主体没下来,这一步就卡住**。

**如果公司主体还在办**,两个过渡方案:
1. 先用个人认证账号跑 MVP 与试点(免费额度够用),主体下来后再迁到企业账号
2. 或由甲方以学校/其单位名义开企业账号,我们作为技术方使用(见下方账号归属)

### 账号归属:建议我们持有

| | 我们持有(推荐) | 甲方持有 |
|---|---|---|
| 成本 | 计入服务费,我们控成本、赚差价 | 甲方直付,我们赚不到这部分 |
| 灵活性 | 可随时切引擎(讯飞→驰声)不惊动客户 | 换供应商要客户配合 |
| 风险 | 用量超支我们承担 → 必须设告警与限流 | 甲方看得见每一笔调用 |

推荐我们持有,**合同里把 API 成本写进服务费**,对客户呈现为"包含在服务里"。同时在服务端做每日调用上限,防止意外超支。

## 三、示范音 TTS(一次性,可稍后)

- 首选 Azure Speech(en-GB Neural HD):azure.com 注册 → Speech 资源 → 拿 Key + Region
- 全境内方案:火山引擎 / 讯飞在线 TTS
- 一次性生成,成本约 ¥27(见 [`04-speech-decision.md`](04-speech-decision.md)),不急,MVP 后期再做

## 四、二期(暂不开)

- 腾讯云 SOE(AI 自由对话打分)
- 驰声 Chivox(全量铺开后谈考试级引擎包年授权)

## 五、拿到讯飞三件套后填这里

```bash
# server/.env.local(已被 gitignore,绝不提交)
XF_APPID=xxxxxxxx
XF_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
XF_API_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

发我之后我接跟读评分,模考模块即可闭环。
