# 项目记忆 · Muzi Studio(木子工作室)

> 给未来会话的上下文。新会话开始前先读这个文件,再读对应目录的 README。
> 最后更新:2026-08-27

## ⚠️ 首要提醒

1. **本仓库当前是公开仓库**,但已装入竞争策略与客户项目方案。
   已建议转私有(Settings → Danger Zone → Change visibility),**确认是否已转,未转则不要再写入敏感内容**。
2. **凭证一律不入库**:统一放 `projects/english-learning/server/.env.local`(已 gitignore)。
   每次提交前用 `git diff --cached | grep -E "sk-|API_SECRET=|ACCESS_TOKEN="` 自查。
3. 会话中出现过的所有 key(DeepSeek / 讯飞 / 火山)**上线前必须重置**。

## 用户与业务定位

- 用户:Muzi(木子),**全栈工程师团队**(一人公司形态),不是教育公司——早期方案曾把定位收窄到教育,已被明确纠正
- 三条业务线:**教育数字化(旗舰)** / AI 产品与开源工具 / 企业定制开发
- 主站 muzi.studio 现有资产:2 款在线产品(ChineseLearn、EQ Trainer)+ 18 个开源 AI 工具 + 公开运营数据
- 人设主线:「不藏失败,不美化数字」——**不走"装大"路线**(曾评估过"(集团)公司"打法并否决),靠可验证的透明建立确定感

## 仓库内容(三块)

| 目录 | 内容 |
|---|---|
| `src/` | 原仓库本体:AI API Calculator(公开工具) |
| `docs/strategy/` | 一人公司签单体系 + 主站改版蓝图 + 首页原型 |
| `projects/english-learning/` | **客户项目:初中英语听说平台「书山英语」**(当前主战场) |

## 当前主战场:书山英语

进度详见 [`projects/english-learning/00-STATUS.md`](projects/english-learning/00-STATUS.md)(每次有进展先更新它)。

**一句话**:给江苏某中学做定制英语听说平台,对标科大讯飞 E听说,21 个班 ≈1365 人,预算 10 万。

### 不可动摇的约束

- **教材是牛津译林版 → 示范音必须英式(en-GB)**,美音会被英语老师当场挑出来
- **教材原文/配套音频版权在译林出版社** → 现用原创同主题短文;要用原文须客户取得授权
- **进校合规三条红线**:教育 App 备案 + 零广告零收费 + **禁止公布学生成绩排名**(所以激励用匿名段位,不用多邻国式真实排行榜)
- **小猪佩奇不能自行嵌播**(版权在 Hasbro/eOne),动画区走自制 AIGC 短片或持牌合作
- 内部谈单话术(名分/预算理由/绑定)**绝不出现在对外文案**,见 `docs/strategy/02` 内外话术分离表

### 已定决策

- 视觉:方向 A(新中式×二次元)皮肤 + 方向 C(徽章收集)机制层;方向 B(电竞 HUD)保留给试点班学生投票
- 命名:产品「书山英语」,slogan「书山有路,AI 为径」,宠物是水墨神兽「墨墨」
- 报价:首年 10 万 = 平台建设 + 首学年服务;**续费按人头 ¥35/生/年**,绝不签永久买断
- 建议首年只做 1-2 个年级(方案 B),而非全校 21 班铺开

## 技术栈与踩过的坑(重要,别重复踩)

### DeepSeek(已跑通)

- 模型:`deepseek-v4-flash`(对话)/ `v4-pro`(批改出题);旧的 `deepseek-chat`/`reasoner` 已于 2026-07 退役
- ⚠️ **v4-flash 默认开思考模式**,对话必须传 `"thinking": {"type": "disabled"}`,否则 token 全烧在推理上;`enable_thinking`/`reasoning` 等写法会被静默忽略
- ⚠️ **上课时段(9-12/14-18)正好是 DeepSeek 计费高峰**,离线任务排错峰(5 折)
- 护栏 system prompt 做固定前缀吃缓存命中(与未命中差约 30 倍)

### 讯飞语音评测 ISE(已跑通)

- 三件套 APPID / APIKey / APISecret,位置:控制台 → **语音扩展** → 语音评测 → 服务接口认证信息
- ⚠️ **英文评测(`ent: en_vip`)不能传 `group` 参数**(那是中文专用),传了必报 `48195 iSEInputAppend error` ← 排查最久的坑
- ⚠️ 文本必须是 `﻿[content]\n` + 待评测文本,BOM 不能省
- 实测有效:文本匹配 4.93/5,文本不匹配 0.03/5,逐词逐音素都有分
- 讯飞英文 TTS 音色(catherine/john/henry)全是**老一代美音**,新一代 `x2_*`/`x4_EnUs_*` 未授权 → **讯飞 TTS 只作兜底,不作示范音**

### 火山引擎 TTS(⛔ 当前阻塞)

- 凭证位置:控制台 → 豆包语音 → 应用管理 → 服务接口认证信息
- ⚠️ 鉴权头是 `Bearer;<token>`(**分号不是空格**)
- ⛔ 阻塞中:`load grant: requested grant not found in SaaS storage`。
  已穷举排除 v1/v3 接口 × 3 cluster × 2 鉴权写法 × 2 resource_id × 6 音色 → **账号侧问题,非代码**。
  控制台矛盾:开通页显示「已开通」但服务详情显示实例「回收」、限额为空。
  排查顺序:费用中心欠费 → 换 `default` 应用(APPID 6842411651)→ 提工单
- ⚠️ 该账号现有英文音色(BV503 Ariana / BV504 Jackson)**都是美式**,英式仍需另购或走大模型音色库

### 示范音架构(关键设计)

**教育内容固定 → 批量预生成 MP3 存 CDN,绝不用实时 TTS,更不用浏览器 Web Speech API**
(安卓平板常缺 en-GB 音色,会退化成机械音)。全量约 17 万字符,一次性成本 ¥0.5–27。
管线:`server/tts-batch.mjs`,provider 可插拔(azure / volc / xf),换引擎删 `audio/` 重跑即可。

## 协作偏好(从对话中总结)

- 要**实话**:能做到什么、做不到什么直说,不吹"全面超越"这类经不起追问的话
- 讨厌"AI 默认审美"(扁平卡片 + emoji 图标),画面要有场景感和角色
- 决策要**给推荐**,不要罗列选项让他挑
- 重视**范围控制**与**别把自己坑死**的商业判断(一人公司视角)
- 长任务要**边做边推送**,不要憋大招

## 工作约定

- 开发分支:`claude/solo-company-sales-strategy-liwhpk`,PR #1(草稿)
- 提交信息用中文,说清「改了什么 + 为什么」,踩过的坑要写进 commit body
- 原型改动后重新发布 Artifact(同 URL 更新):
  - 书山英语原型:`2de3c70d-84a8-4321-976f-2f2c4f4af6cf`
  - 甲方方案书:`251211c2-4e4d-4ab8-bb09-ac894dda0fbe`
  - 主站首页原型:`2c8be244-0fbf-4127-8d45-4c11d8571e75`
